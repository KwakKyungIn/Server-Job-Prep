syntax = "proto3";
package Protocol;

import "Enum.proto";
import "Struct.proto";

// ============================================================
// 1. DB Agent Åë½Å (·Î±×ÀÎ)
// ============================================================
message S2S_REQ_LOGIN {
    string name = 1;
}

message S2S_RES_LOGIN {
    bool success = 1;
    uint64 playerId = 2;
}

// ============================================================
// 2. Chat Server Åë½Å (Ã¤ÆÃ Áß°è)
// ============================================================
// GameServer -> ChatServer (ÀÌ°Å »Ñ·ÁÁà)
message S2S_REQ_BROADCAST_CHAT {
    uint64 playerId = 1;
    string name = 2;
    string message = 3;
}

// ChatServer -> GameServer (»Ñ·È¾î)
message S2S_RES_BROADCAST_CHAT {
    bool success = 1;
}